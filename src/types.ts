export type User = {
  first: string;
  last: string;
  city: string;
  phone: string;
  email: string;
};

export type ErrorMessages = {
  first: string;
  last: string;
  city: string;
  phone: string;
  email: string;
};
